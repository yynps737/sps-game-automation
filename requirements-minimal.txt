# 最小依赖 - 真正需要的库
# Python 3.11.8+

# ============== 核心必需 ==============
opencv-python==4.9.0.80         # 图像识别
numpy==1.26.4                   # 数组操作
PyYAML==6.0.1                   # 配置文件
loguru==0.7.2                   # 日志系统
Pillow==10.2.0                  # 图像处理

# ============== 可选 ==============
# PyQt6==6.6.1                  # GUI界面（暂时不需要）
# adbutils==2.1.1               # 不需要，直接用subprocess调用adb